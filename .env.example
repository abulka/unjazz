# GitHub Configuration

# Use GitHub Releases for audio hosting
# Set to 'true' to upload MP3s to releases
# Set to 'false' to use local paths (dev mode)
USE_GITHUB_RELEASES=false

# GitHub Token for uploading to releases (required if USE_GITHUB_RELEASES=true)
# Get from: https://github.com/settings/tokens
# Required scopes: repo
GITHUB_TOKEN=

# Repository for audio hosting (required if USE_GITHUB_RELEASES=true)
# Format: username/repo-name
GITHUB_REPO=

# Release tag for audio files
RELEASE_TAG=v1.0

# Base path for GitHub Pages deployment
# Should match your repository name if deploying to GitHub Pages
# Example: /unjazz for https://username.github.io/unjazz/
BASE_PATH=/unjazz

# CORS Proxy Configuration (for Safari compatibility with GitHub Releases)
# After deploying Cloudflare Worker, add your worker URL here
# Example: https://unjazz-audio-proxy.your-subdomain.workers.dev
CORS_PROXY_URL=

# Debug Mode
# Set to 'true' to show waveform debug overlays (touch indicators, coordinates)
VITE_DEBUG_WAVEFORM=false
